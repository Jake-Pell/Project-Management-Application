- User has too much, needs mostly getters

- addUser needs params that looks like the ones in user

- move JSON files out of lib

- put column object instead of ID

- Nest comments inside task & project, nest comments instead of having replies

- add Main

- Only work on getUsers until it works by Wed.

- *** MAIN TAKEAWAYS*** Clean up user class, finish user list, move JSON, consolidate JSON, don't worry about projects yet